<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nf.co.mohatram.grescrabble">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GreQuestionApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".JoinGameActivity" />
        <activity android:name=".HostGameActivity" />
        <activity android:name=".QuestionViewerActivity" />
        <activity android:name=".ResultDisplayActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="Results"/>
    </application>

</manifest>